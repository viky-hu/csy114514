# Modules Index

## 冻结前端契约（A.3）

Evaluation / Anatomy 的冻结边界统一记录在 [`frozen-frontend-contracts.md`](frozen-frontend-contracts.md)。AI 或开发者默认不得修改 `evaluation-types.ts` 引用的 18 种 `EventType`、`parseEvent()` / `reduceEvaluationEvent()` SSE 核心逻辑、`BatchProgressPanel.tsx` 进度语义、`EvaluationTerminal` 事件格式、Anatomy 的 `RiskPattern` 定义、GSAP 与业务逻辑的隔离关系，或 `evaluation-session.ts` 的 `sessionStorage` 持久化语义。`EventType` 的权威来源是 `app/lib/contracts/backend-openapi.json` 生成的 `app/lib/contracts/backend-api.d.ts`；前端不得另建平行枚举。当前 `EvaluationTerminal` 实现在 `EvaluationRunWorkspace.tsx` 内，不应为此改变事件流边界。

## apps/main-platform

Main Next.js App Router frontend. It owns routing, app layout, first-party CSS, and same-origin BFF route handlers.

- `app/layout.tsx`: root HTML shell and global style imports.
- `app/page.tsx`: initial login entry screen and same-route switch into the main workspace after the local Agent loading flow completes.
- `app/api/**/route.ts`: BFF layer for frontend-to-server boundaries.
- `app/windows/login/LoginIntroWindow.tsx`: Window 1 login surface copied from `C:\Users\Admin\final-main\final-main`, including the viewport-pinned SVG root, mouse-following band, inverted text overlay, auth-state CTA copy, CTA animation, Escape close behavior, mock sign-in close flow, login-gated scroll band expansion with top-position pointer unlock and first-scroll preservation of the current band width, plus the fixed second-page Agent entry prompt, local draft overlay, and locked mock loading state after either Agent action. Its pointer-follow and leave-collapse interactions are gated to the top pointer mode so non-top scroll/second-page/loading states cannot collapse the full blue band. The loading overlay consumes shared Chinese tips, while `login-loading-tip-sequence.ts` owns a stable per-Agent 7-10-second mock plan and future phase/complete/failed event boundary; `login-loading-session.ts` rejects stale session/tip callbacks and duplicate exit completion, and the first tip begins only after the overlay reveal completes. `LoginSplitLoadingTip.tsx` keeps each visible line inside one stable SplitText presenter: it prepares text while hidden, plays capped per-character entrance, holds without rebuilding DOM, and fades the same split line out before the controller advances. `login-band-motion-controller.ts` is the login-only owner of shared SVG band geometry: it serializes pointer, opening, open, closing, and scroll transitions, writes through private tween state so stale GSAP callbacks cannot restore a hover width, and makes the opening path settle at the panel target on completion or interruption.
- `app/windows/login/login-band-motion-controller.ts` and `login-band-motion-controller.test.ts`: login-window-private GSAP motion ownership boundary and focused Node tests. They are not a cross-window animation abstraction; new login-band motion must enter through this controller rather than directly tweening the rendered SVG geometry.
- `app/windows/login/login-loading-tip-sequence.ts` and `login-loading-session.ts`: login-only loading timing boundaries. The sequence derives deterministic dwell variation from shared tip ids, produces a non-repeating boot subset whose cumulative display duration is 7-10 seconds, and keeps future `phase`, `complete`, and `failed` progress signals pending until the current whole-line exit boundary. The session controller keeps the frontend-only “稍后再说” mock independent from backend availability and invalidates callbacks from superseded sessions. `LoginSplitLoadingTip` retains one React component identity across text changes and does not use `revertOnUpdate`; SplitText teardown only happens while the line is hidden, so visible text never flips back to an unsplit React text node.
- `app/windows/login/LoginForm.tsx`: Window 1 mock login/register form. During MVP it preserves the SVG-styled input surface while treating login and apply-register actions as unconstrained successful sign-in events.
- `app/windows/login/AgentConnectDraft.tsx`: Second-page Agent Manifest configuration draft. It collects only fields backed by the frozen backend `AgentManifest`, shows a security/profile precheck, and keeps JSON details collapsed without exposing adapter-only endpoint/template inputs before that contract is frozen. The profile preview keeps its risk and validation lists content-sized at their 44px row floor; when the available height is short, the preview scrolls independently instead of allowing list content to overlap.
- `app/windows/main/MainWindow.tsx`: Main workspace SVG shell entered after the Agent loading overlay fades out. It starts from a full-screen blue SVG cover, then uses a GSAP timeline to collapse it into a horizontal separator line and reveal real SVG white top/main surface rectangles for later workspace content. It also owns the SVG-aware left navigation placement, tightened sidebar width boundary, default dashboard selection, content-region replacement, page fade-out/fade-in swapping, and intro timing for the main workspace; the active navigation target stays attached to the swap lifecycle so forward and reverse page navigation share the same fade-out, keyed replacement, and fade-in completion path, while interrupted or completed motion restores the content region and keyed page shell to a visible, interactive steady state. The content region is intentionally close to the sidebar menu body while retaining larger right/bottom safety insets at 100% browser zoom.
- `app/windows/main/settings/**`: Focused Account Center/设置 content for the main workspace. `AccountSettingsWorkspace` owns profile loading, display-name dirty state, two-step avatar selection/crop/save state, password-dialog visibility, and local logout cleanup; `AccountAvatarCropDialog` uses `react-easy-crop@5.5.4` for constrained 1:1 drag/zoom interaction, while `avatar-image.ts` owns bounded Canvas rendering, the debounced 256px preview, and the 512px PNG upload file. Source Object URLs and preview Data URLs are temporary browser-only editor state: crop confirmation marks the profile dirty, and only the existing Save Profile action may upload through the account repository. `AccountPasswordDialog` owns its accessible focus loop and password form state; `account-settings.ts` keeps profile normalization and validation pure; `account-repository.ts` is the browser-side same-origin repository. The module must remain limited to identity, access security, and logout: Agent configuration, risk rules, TestCases, reports, notifications, themes, language, and display preferences stay in their owning modules. It reuses `MainWindow`'s keyed 0.22s exit/0.38s entrance transition and adds only scoped section/Dialog motion with direct reduced-motion final states.
- `app/api/account/**` and `app/lib/server/account-auth.ts`: Same-origin BFF boundary for MiA-RAG account profile, avatar, password, and logout operations. The browser supplies its bearer token only to these same-origin routes; the upstream origin is read from server-only `MIA_RAG_AUTH_URL`, and the csy platform never stores password content or exposes the authentication-service address through `NEXT_PUBLIC_*`. Missing authentication and unavailable upstream services use the shared parseable `{ error: { code, message, details } }` response shape.
- `app/windows/main/MainLineSidebar.tsx`: React port of the Vue Bits line-sidebar interaction pattern for the main workspace. It keeps pointer-proximity RAF smoothing, active-item state, semantic nav buttons, and project-owned CSS without adding Vue or shadcn-vue runtime dependencies.
- `app/windows/main/overview/**`: Default Dashboard/总览 content for the main workspace. It adapts `shared/fixtures` AgentProfile, AttackGraph, and EvaluationReport data into a local `OverviewViewModel`, including explicit R4 step metadata for `SOURCE -> AGENT -> MEMORY -> AGENT -> TOOL`. `OverviewDashboard` owns page composition and the wide-graph/narrow-side-stack balance, while `OverviewR4Graph`, `overview-r4-layout.ts`, and `overview-r4-svg-viewport.ts` own the private `1000x440` graph coordinate space, the dedicated left layer-info lane with independent foreground SVG text and no label container, three taller horizontal semantic lanes, fixed icon/title/caption node content slots, node-bounds-derived x-axis hover hot zones whose frosted-column left/right edges exactly match each node rectangle's measured horizontal bounds after the SVG's rendered `xMidYMid meet` viewport offset/scale is applied, GSAP-driven frosted-column opacity and rail emphasis without a moving sheen block, arrowless orthogonal SVG route anchors aligned to node rectangle edge centers, scoped GSAP timelines, DrawSVG-style hover outlines, and reduced-motion direct-set behavior. HTML hover strips, popover hitboxes, and pointer hit testing must use the rendered SVG matrix/content viewport rather than frame-wide percentages so large monitors, laptop screens, and narrow containers stay aligned without `preserveAspectRatio="none"`. The graph remains a dashboard overview component rather than the full Anatomy attack-graph page, and the fixture-shaped adapter remains the only data boundary so a later BFF-backed repository can replace the source without changing the visual layer.
- `app/windows/main/anatomy/**`: Attack Graph/攻击图谱 content for the main workspace. It is the full risk-path workbench described by section 4.3, separate from both the dashboard R4 thumbnail and the Security Profile boundary confirmation page. `anatomy-data.ts` owns the normalized preview/live view model, status split, selected-path defaults, R4 TestCase/AttackSeed verification metadata, and the rule that `risk_path_ids` only means 待验证 while `evaluationReport.findings` is the only 已验证 source. `anatomy-repository.ts` owns the internal `MockAnatomyRepository` / `ApiAnatomyRepository` boundary: the page first renders fixture preview data, then tries same-origin `GET /api/agents/{agentId}/graph`, converts valid payloads into live view models with no report-backed verification, and falls back to the mock repository on 404/503/invalid payloads without calling `POST /agents` or the FastAPI backend directly. `AnatomyGraph` owns the SVG + GSAP workbench, compact data-source marker, path cards aligned below the map frame, right-side path inspector plus selected-node detail inspector, safety-label chips, evaluation handoff, and empty state; the visible preview/live switch panel is intentionally omitted in the current frontend build so graph space stays dominant while still exposing fixture/API source state. `anatomy-graph-layout.ts` owns the private `1000x520` five-stage attack-chain coordinate space (`入口 -> 初次解析 -> 持久化 -> 二次唤起 -> 外发动作`), deterministic curved R1/R2/R3/R4 route geometry, one visible curve per source/target node pair, uniform `150x88` node rectangles, edge-center node anchors, selected-route endpoint node derivation, bottom phase-rail placement, and human-readable visual node aliases such as 恶意网页, Agent 解析, 长期记忆, 二次唤起, and 发送邮件. Shared edges list every matching risk path in `pathIds`; for example `Agent 解析 -> 发送邮件` is a single curve shared by R1 and R3 rather than two overlapping route drawings. Active node emphasis is the union of explicit path steps and every selected route's source/target endpoints, so nodes such as `读取邮件` stay highlighted whenever a selected R path visibly connects through them. The visual coordinate system applies a shared `+48x/+28y` offset to nodes, routes, control points, phase labels, and hitboxes so the graph's content mass sits closer to the map-frame center instead of drifting to the upper-left. The Anatomy graph uses the outer map panel as its only visible frame and stage boundary; the two-row R1/R2/R3/R4 path-card grid stretches through the remaining left-column height while keeping a small bottom safety inset so the lower card borders stay visible, and the path cards plus right inspector keep a slightly larger reading scale than the initial preview build. It must not reintroduce a second inner SVG rectangle, detached internal width cap, duplicate visible edges for the same node pair, non-uniform Anatomy node rectangles, `preserveAspectRatio="none"`, or scaling-based active node highlight.
- `app/windows/main/evaluation/**`: Real Evaluation Run/Report vertical slice. `EvaluationWorkspaceProvider` owns validated sessionStorage recovery, the evaluation snapshot, TestCase catalog selection, event cursor, SSE lifecycle, and event reducer above the keyed content shell; it replaces a restored run and selection when the attack-graph handoff event arrives. Individual TestCase checkbox changes use a Provider-owned functional toggle against the latest selection, while `TestCaseSelector` anchors its visually hidden native control to the selected row so focus and selected decoration do not move the workspace layout. `TestCaseSelector` filters and multi-selects the backend-provided 72-case catalog, and its `开始批量测评` action creates the run and transitions to the execution view. `BatchProgressPanel` derives every selected row from persisted `TEST_STARTED` / `TEST_COMPLETED` events and displays deterministic totals, row states, and errors only; rotating loading-tip copy is intentionally absent beside the batch totals. Its execution action is `开始测评`, with the adjacent ArrowLeft control returning to TestCase selection without discarding the current selection. `EvaluationTerminal` keeps filtering, search, copy, and event rendering unchanged, but stays visually empty when no events match instead of showing a rotating loading tip. The legacy `tc_pipi_001` R4 rail remains available, uses `finalJudgeVerdict` to display the persisted latest `JUDGE_DECISION` PASS/FAIL outcome separately from its evidence-derived risk state, and exposes the same non-running TestCase-reselection action as the batch surface while retaining its current selection. Generic runs use the batch surface and a redacted read-only terminal. `ReportSummaryPanel` renders optional backend `summary` dimensions for R1-R4, OTHER, risk type, and severity; it does not recompute results client-side. Selection and execution content stay inside a height-constrained view shell: the shell/content/selector grid chain must pass `height: 100%` and `min-height: 0`, the selector footer remains outside the list track, and only `.evaluation-selector-list` owns vertical scrolling.
- TestCase selector focus contract: row hover and selected decoration remain independent from focus indication; mouse clicks do not leave a row-level outline, while keyboard-visible checkbox focus is exposed through `.evaluation-selector-row:has(> input:focus-visible)`.
- `app/windows/shared/loading-tips.ts`: Cross-window Chinese loading-tip catalog and selector used by login Agent entry, main Agent interface, and evaluation run/report states. It maps existing frontend run/status/event state into `boot`, `preflight`, `running`, `judging`, `reporting`, `idle`, and `error` phases without adding backend contracts, changing layout geometry, or fabricating evaluation events; login-only timing remains outside this shared data contract.
- `app/api/evaluations/**`, `app/api/test-cases/route.ts`, and `app/lib/contracts/backend-api.d.ts`: Same-origin Node BFF for TestCase catalog reads plus evaluation creation, start, snapshot, event stream, trace, and report. `backend-openapi.json` is the FastAPI generation input; `backend-api.d.ts` is generated with `openapi-typescript` and must be regenerated after the public API changes. SSE proxying preserves `Last-Event-ID`, request cancellation, no-cache/no-transform, and anti-buffering headers; backend URLs remain server-only.
- `app/api/evaluations/comparisons/**`, `EvaluationComparisonRunWorkspace.tsx`, `EvaluationComparisonWorkspace.tsx`, and `comparison-report-summary.ts`: same-origin BFF and split presentation boundary for the fixed `llm-agent-v0` Bare versus `defended-llm-v0` comparison mode. Creation performs both child-run prechecks and leaves the comparison `queued`; the explicit comparison start route moves both ready child runs into `running_parallel` and the comparison scheduler claims them together. `EvaluationWorkspaceProvider` owns comparison-id session recovery, side-local SSE event reduction/deduplication, side retry, mock playback, and report loading. The run workspace contains only dual progress and terminal views with one global action group; its return/start/report controls sit to the left of the Bare/Defended mode badge. The report workspace is an application-reading security brief: its pure frontend summary derives comparable coverage, both safety pass rates, percentage-point change, five transition groups, R1-R4/OTHER results, and risk-prioritized ledger rows only from the returned comparison rows, Finding fields, and TestCase catalog. Only explicit PASS/FAIL pairs enter the rate denominator; incomplete rows remain visible as coverage warnings. It must not infer performance, cost, latency, strategy-hit causes, or add report fields to the BFF contract. Comparison events wrap the frozen event payload with `side`, `run_seq`, and a comparison cursor without changing the 18-event single-run contract; the provider closes normal terminal EOF, delays transient warnings, manually retries unavailable BFF responses, and resets the aggregate cursor when a child `run_id` changes; the report entry is exposed only after both child runs complete successfully.
- `EvaluationWorkspaceProvider` comparison return invariant: the global return action clears the active comparison session and restores `evaluationMode` to `single` without clearing the selected TestCase IDs. Any selector action callback that guards on the mode must subscribe to the current mode state, so the visible single-mode selector and the creation path cannot diverge after a mode transition.
- `app/lib/server/backend.ts` and `app/api/agents/[agentId]/graph/route.ts` / `app/api/evaluations/[evaluationId]/report/route.ts`: Same-origin BFF boundary for live attack-graph and evaluation-report connections. Graph proxy failures return the shared `{ error: { code, message, details } }` shape so frontend repositories can fall back cleanly. The frontend does not call the FastAPI backend directly or expose backend secrets through `NEXT_PUBLIC_*`.
- `app/windows/shared/evaluation-handoff.ts`: Frontend handoff store for carrying `agentId`, `riskPatternId`, and `testCaseId` from the attack graph into the evaluation/run page without changing backend contracts. Writes also dispatch `csy:evaluation-handoff`, so a mounted evaluation provider adopts each new path selection rather than retaining a previous batch.
- `app/windows/main/shared/graph-svg-primitives.ts`: Main-window SVG graph primitives shared by overview and profile graph modules. It owns centered-rectangle bounds and clockwise rounded-rectangle path generation so feature graphs reuse the same SVG node geometry instead of maintaining near-duplicate helpers.
- `app/windows/main/profile/**`: Security Profile/安全画像 content for the main workspace. It adapts the same fixture-shaped AgentProfile and AttackGraph data into a local `SecurityProfileViewModel`, keeping the boundary confirmation page separate from the later attack-analysis page. `SecurityProfileGraph` owns the Agent-centered boundary map, selected-node inspector, overview-style HTML hot zones, HTML node hitboxes, foreground SVG column text, DrawSVG hover outlines, and scoped GSAP reveal/highlight behavior, including the page-level reveal choreography that brings the header, map, inspector, and footer in after mount; `security-profile-graph-layout.ts` owns the private `1000x500` SVG coordinate space, explicit inner graph boundary, fixed node bounds, ViewBox-derived hover bands, edge-center node anchors, deterministic curved route segments, and coordinate hit testing while using shared main-window SVG primitives for node geometry. The profile graph uses overview-isomorphic transparent rectangular nodes (`path surface + DrawSVG outline + lucide icon + centered label/caption`) and four equal-gap column bands (`90-242`, `310-484`, `552-712`, `780-944`) whose SVG bands, HTML hover strips, hitboxes, and route anchors are derived from the same ViewBox data. Unlike the overview's single attack-chain path, profile routes now share one blue-purple gradient stroke for all curves and hover outlines while preserving routeTone metadata only as semantic labeling, so default relationships stay quiet without splitting the palette. The profile graph must not use pixel-positioned hover strips, circular marker/initial nodes, horizontal hover translation, full-width layout animation, or `preserveAspectRatio="none"`; node hover keeps x coordinates stable with a shared y/scaleY/outline treatment. The graph keeps only the five main semantic routes (`browser -> agent`, `email data -> email.read`, `agent -> email.read`, `agent -> email.send`, `agent -> memory`) and does not add the fixture's `memory -> agent` back edge to avoid loop-like visual clutter. The module does not invent fixture-missing delete/DENY tools, add BFF routes, or introduce another animation/style dependency.
- `app/windows/**`: feature-owned window modules aligned with the final repository shape.
- `app/styles/**`: app-level and feature-level CSS.

## packages/ui-components

Shared React UI primitives consumed by application packages. Keep components small, typed, and style-compatible with the app token system.

## packages/configs

Shared TypeScript configuration package for Next.js apps and React libraries.

## 2026-08-12 Agent Initial Interface Update

- `app/api/agents/route.ts` and `app/api/agents/[agentId]/route.ts`: same-origin BFF handlers for backend `POST /agents` and `GET /agents/{agent_id}`. They forward generated-contract `AgentManifest`/profile traffic to the backend service and return the shared parseable backend-unavailable error shape on failures.
- `app/windows/shared/agent-config.ts`: shared Agent Manifest draft adapter used by login and main-window configuration surfaces. It owns the CorpMate preset, frozen `AgentManifest` builder, backend profile hydration, and risk-asset preview helpers. The editable surface intentionally excludes endpoint, method, headers, request template, and response path until the backend freezes an adapter persistence contract. `memory` is represented through `manifest.memory` rather than `data_sources`, matching the current backend graph semantics.
- `app/windows/login/AgentConnectDraft.tsx`: controlled shared Agent configuration form. Login uses it to submit real Manifest data before entering the main window; the main 初始接口 page reuses it for later configuration or overwrite. Both surfaces share the content-first preview row constraint and independent short-viewport scrolling.
- `app/windows/main/agent/AgentInterfaceWorkspace.tsx`: main-window 初始接口 workspace. It loads the current Agent profile, keeps recoverable errors visible while preserving editable defaults, posts only frozen Manifest data to `/api/agents`, and asks `MainWindow` to restart into the dashboard on success.
- `app/windows/main/MainWindow.tsx`: owns `activeAgentId`, passes it into overview/anatomy/evaluation workspaces, clears the persisted evaluation run session after Agent reconfiguration, and replays the existing blue loading-to-line intro after a successful save. It also owns the `AgentProof` main-window SVG wordmark: the wordmark aligns to the final separator's `insetX`, derives its baseline from `lineTop - 0.55 * cmY`, and joins the existing intro timeline at `1.12s` so interruption, resize, replay, and reduced-motion paths all converge on the same settled state.
- `app/windows/main/overview/**`: overview now prefers live `GET /api/agents/{agentId}` and `GET /api/agents/{agentId}/graph` data for Agent/profile/graph summary, while the evaluation report remains fixture-backed until the later Stage 2 report/list API boundary is complete.

## csy——全智赛/backend

- `一键启动.bat`: local Windows launcher for the FastAPI service. It validates the copied virtual environment, falls back to the machine `python` executable when the venv points to a stale absolute path, reuses the venv site-packages directory, checks both the FastAPI runtime and the attack-graph `networkx` dependency before printing the service banner, and supplies the development-only `TRACE_FINGERPRINT_KEY` required by the application lifespan.
- `backend/app/services/evaluation_service.py` and `backend/app/sandbox/composite.py`: evaluation creation validates the selected registered Agent (with the reference CorpMate fallback), passes its declared tool permissions into execution and the generic judge, and records permission denials as redacted trace events. Generic batches convert an unexpected per-case execution failure into `TEST_COMPLETED: ERROR` for every unfinished selected TestCase and still persist a degraded report, keeping the batch/report contract terminal and inspectable.

## 2026-08-20 Evaluation Stage 3 A1/A2 Frontend Adapter

- Ownership: `app/windows/main/evaluation/evaluation-agent.ts` owns the private three-option Evaluation Agent metadata and neutral unknown-Agent fallback; `EvaluationWorkspaceProvider.tsx` owns the local `evaluationAgentId` and display-only `email.send` confirmation queue. `EvaluationRunWorkspace.tsx` owns the run-lifecycle visibility derivation for the fixed LLM overlay. `EmailConfirmationDialog.tsx` owns the confirmation's local focus trap and entry/exit presentation lifecycle, while Provider callbacks remain the only decision/queue boundary. `TestCaseSelector.tsx`, `EvaluationInferenceStatus.tsx`, `EmailConfirmationDialog.tsx`, `EvaluationAgentBadge.tsx`, `EvaluationRunWorkspace.tsx`, and `EvaluationReportWorkspace.tsx` consume those boundaries. The run-page header displays the Agent badge and one fixed Agent-specific inference state; rotating loading copy and per-turn inference monitoring are intentionally absent there.
- Presentation boundary: `EvaluationRunWorkspace.tsx` keeps TestCase selection and evaluation execution inside one view shell. GSAP fades only that shell when the derived `selecting`/`running` mode changes; the latest run/event snapshot keeps the outgoing running view coherent while reselection is being rendered. The shell and its content grid preserve a definite height for the selector's nested list, so the `开始批量测评` footer remains visible while `.evaluation-selector-list` handles overflow. `EvaluationInferenceStatus` is positioned as a non-interactive header overlay and never contributes a third grid row or changes evaluation state.
- Boundary: the Evaluation selector controls only `POST /evaluations.agent_id`. It does not change `MainWindow.activeAgentId`, Agent configuration, the attack graph, or graph reload behavior. Anatomy handoff may select a TestCase but cannot overwrite the current Evaluation Agent. Existing `parseEvent()`, `reduceEvaluationEvent()`, `BatchProgressPanel`, `EvaluationTerminal`, and `evaluation-session.ts` remain the frozen event/session boundaries.
- Event and status semantics: LLM inference details remain available to report/terminal derivations from persisted `AGENT_INVOKED` / `AGENT_RESPONDED` events and their explicit batch or legacy R4 correlation fields. The one-second timer refreshes elapsed display time only; it does not create events, infer completion, or compute a verdict. The header overlay lifecycle is separate: `isInferenceRunActive(status, isStarting)` keeps the non-interactive white overlay visible from start/queued/running through the whole run, keeps one fixed Agent-specific title `{meta.shortLabel} 推理中…`, and fades it out only for terminal statuses. `EvaluationInferenceStatus` does not consume active inference, TestCase, turn, elapsed-time, or event-gap data, so the overlay content never jumps during a run.
- Development mock boundary: `?evaluationMock=1` is read at the page entry and passed into `EvaluationWorkspaceProvider` after login. The provider uses in-memory TestCase, run, event, trace, and report fixtures with the same frozen contracts, while bypassing only bootstrap/API/SSE calls; it never writes the evaluation session key or intercepts the global network layer. Removing the query parameter restores the real backend path.
- Confirmation semantics: the `email.send` confirmation Dialog is an MVP visualization. It recognizes only unconfirmed `TOOL_CALLED` events, deduplicates by `event_id`, redacts arguments except for a safe recipient label, records local decisions, and never calls a confirmation endpoint, changes the reducer, blocks the runner, or changes the server report/Judge result. Backend sandbox authorization remains authoritative.
- Contract boundary: `defense_summary`, `agent_type`, and `defense_labels` are not in the current generated contract. Frontend must not create parallel handwritten wire fields; after backend support lands, regenerate `backend-api.d.ts` from `backend-openapi.json` and integrate those fields separately.
- Validation: run the focused evaluation unit/structure tests, including the confirmation Dialog animation/style contract, `pnpm -C apps/main-platform run type-check:app`, lint, build, and desktop/mobile/reduced-motion manual checks for the selector, inference status, confirmation Dialog, and Agent badges.

## 2026-08-23 Evaluation Inference Overlay Lifecycle

- Ownership: `inference-status.ts` owns the pure run-lifecycle predicate; `EvaluationRunWorkspace.tsx` passes that predicate result to `EvaluationInferenceStatus.tsx`. The overlay intentionally has no active-inference detail dependency.
- Presentation: the header overlay is compact, pure white, regular-weight, weakly shadowed, and absolutely positioned without adding a page grid row. Its width is capped below the prior half-header span, and a right-side `X` control dismisses the current overlay locally without changing the run. CSS enter/exit motion runs once at the run boundary; reduced motion keeps the final visibility state without movement.
- Boundary: `ready` and all terminal statuses hide the overlay; `preflighting`, `queued`, `running`, and the start request show it. The overlay remains present even when no inference event is available, with the same fixed Agent-specific `{meta.shortLabel} 推理中…` title. It never displays TestCase, turn, elapsed-time, or event-gap changes. Non-LLM Agents remain hidden.
