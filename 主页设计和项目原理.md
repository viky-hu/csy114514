# 主页面方案 v0.2

## 面向前端开发的系统逻辑加强版

## 1. 主页面要讲清楚一个完整故事

这个平台不是普通的数据后台，也不是把很多安全功能并排放在一起。

它讲的是一条连续的“智能体上线体检”流程：

```text
平台先认识 Agent
        ↓
找到可能存在的问题
        ↓
实际运行测试验证问题
        ↓
给出结论、证据和修复建议
```

对应到页面就是：

```text
总览
  ↓
安全画像
  ↓
攻击图谱
  ↓
测评运行
  ↓
测评报告
```

用户每进入下一页，都应该感觉自己正在完成体检的下一步，而不是切换到另一个无关的后台模块。

---

# 2. 页面设计需要体现的三个系统原理

前端不需要理解复杂的智能体安全技术，但需要理解下面三个产品逻辑。

## 2.1 先认识，再检测

平台不能一接入 Agent 就直接开始攻击。

它首先要弄清楚：

```text
Agent 会做什么？
能读取什么内容？
可以调用哪些工具？
哪些操作需要用户确认？
有没有长期记忆？
```

因此先有“安全画像”，再有“攻击图谱”。

页面语言可以理解为：

> 先建立 Agent 的体检档案，再决定应该检查哪些位置。

---

## 2.2 发现风险，不等于风险已经发生

攻击图谱展示的是：

> 从结构上看，这里可能存在一条危险路径。

例如：

```text
恶意网页
→ Agent
→ 长期记忆
→ Agent
→ 发送邮件
```

这时平台只能说：

> 这条路径值得检查。

不能直接说攻击已经成功。

所以攻击图谱中的路径应先显示为：

```text
潜在风险路径
```

用户点击“验证这条路径”后，平台进入测评运行页，实际尝试运行测试。

测试成功后，路径状态再变成：

```text
已验证风险路径
```

这是整个平台最重要的视觉变化之一：

```text
潜在风险
   ↓ 动态测试
已验证风险
```

---

## 2.3 每个结论都要有证据

平台不能只给出：

```text
安全分数：43
风险等级：严重
```

用户还需要知道：

```text
测试输入了什么？
Agent 做了什么？
调用了哪个工具？
哪一步违反了规则？
为什么这件事危险？
```

因此：

- 测评运行页负责展示过程；

- 测评报告页负责整理证据和结论。

这也是为什么“运行时间线”和“报告证据”不需要独立成为导航页面，它们分别属于测评运行和测评报告。

---

# 3. 主页面整体结构

建议继续使用：

```text
顶部状态栏
+
左侧步骤导航
+
中央内容区
```

## 3.1 顶部状态栏

顶部需要始终回答：

```text
现在测的是谁？
进行到哪一步？
系统是否正常？
```

建议包含：

```text
[产品名称]

[当前 Agent]
CorpMate · 已完成画像

[流程进度]
画像 ✓ → 图谱 ✓ → 测评 ● → 报告 ○

[系统状态]
服务正常

[导出报告]

[头像]
```

开发专用的 Mock 开关可以保留，但比赛演示和正式版本中隐藏。

---

## 3.2 左侧导航

继续保留五项：

```text
01 总览
02 安全画像
03 攻击图谱
04 测评运行
05 测评报告
```

建议把它做成“步骤导航”，而不是普通后台菜单。

每一步可以有三种状态：

```text
未开始
进行中
已完成
```

用户可以返回查看已经完成的步骤，但不能跳过必要步骤直接生成报告。

---

# 4. 五个页面的加强设计

## 4.1 总览 Dashboard

### 本页回答

> 当前 Agent 的体检进行到了哪里，下一步该做什么？

### 建议区块

#### 当前 Agent

显示：

```text
名称
一句话描述
接入方式
当前状态
```

例如：

```text
CorpMate

能够浏览网页、处理邮件并保存长期记忆的企业办公助手。

接入方式：平台参考 Agent
状态：发现潜在高风险路径
```

#### 流程进度

```text
安全画像        已完成
攻击图谱        已完成
动态测评        进行中
测评报告        等待生成
```

#### 核心结果

建议只展示最重要的四项：

```text
当前上线评分
潜在风险路径数量
已验证风险数量
最高风险
```

这里要注意区分：

```text
潜在风险：图谱分析发现，但尚未实际验证

已验证风险：测试已经真正打通
```

#### 下一步操作

页面中最明显的按钮只保留一个：

```text
继续验证高风险路径
```

或者：

```text
开始 Agent 解剖
生成测评报告
```

按钮内容根据当前进度自动变化。

---

## 4.2 安全画像 Profile

### 本页回答

> 平台是怎么理解我的 Agent 的？理解得对不对？

### 页面核心

安全画像不是复杂的技术分析结果，而是一张由平台整理、用户确认的“Agent 能力档案”。

建议分为四个区域。

#### 能力

```text
阅读邮件
发送邮件
浏览网页
保存用户偏好
```

#### 外部内容来源

```text
网页
邮件
用户输入
```

其中网页、外部邮件等需要标记为：

```text
可能包含不可信内容
```

不用直接写“攻击源”，避免术语过重。

#### 记忆

单独设计一张记忆卡片：

```text
长期记忆：已启用

Agent 可以把信息保存到之后的对话中继续使用。
```

并给出简单提醒：

> 长期记忆可以提升体验，但也可能让错误或恶意内容持续影响之后的操作。

#### 工具权限

```text
读取邮件        允许

发送邮件        需要确认

删除邮件        禁止
```

本页核心操作：

```text
确认画像并分析风险
```

页面重点不是“看”，而是让用户确认：

> 平台后续会按照这份权限规则判断 Agent 是否越权。

---

## 4.3 攻击图谱 Anatomy

### 本页回答

> 从 Agent 的结构上看，问题可能从哪里进入，又可能影响到哪里？

这是整个平台最有辨识度的页面。

### 中央图谱

展示：

```text
外部来源
Agent
工具
记忆
敏感数据
```

不要求用户理解节点类型名称，只需要通过视觉区分：

```text
内容从哪里来
Agent 在哪里处理
信息保存在哪里
最终可能执行什么操作
```

例如：

```text
恶意网页
   ↓
CorpMate
   ↓
长期记忆
   ↓
CorpMate
   ↓
发送邮件
```

### 风险路径状态

建议使用两种状态。

#### 潜在风险

```text
结构上可能存在
尚未经过动态测试
```

视觉可以使用：

```text
虚线
较弱高亮
“待验证”标签
```

#### 已验证风险

```text
动态测试已经成功打通
```

视觉可以使用：

```text
实线
强高亮
“已验证”标签
```

具体颜色和动画由前端决定。

### 右侧风险说明

用户选择一条路径后，显示：

```text
风险名称

风险等级

大白话解释

为什么值得测试
```

例如：

> 网页中的恶意内容可能被 Agent 保存到长期记忆，并在之后的正常对话中诱导 Agent 发送未经用户确认的邮件。

主按钮：

```text
验证这条风险路径
```

点击后进入测评运行页，并自动选择对应测试。

---

## 4.4 测评运行 Run

### 本页回答

> 平台现在正在如何验证这条风险路径？

建议保留左、中、右三部分，但三栏不一定必须固定宽度，前端可以根据视觉效果调整。

### 左侧：测试任务

显示：

```text
网页内容注入测试       已通过

长期记忆污染测试       运行中

未经确认发送邮件       等待中
```

每条测试只有四种状态：

```text
等待
运行中
通过
发现问题
```

### 中间：动态过程

这里是第二个核心展示区域。

建议提供两种视图：

```text
攻击路径视图
过程时间线
```

#### 攻击路径视图

随着真实运行事件逐步点亮：

```text
网页 ✓
 ↓
Agent ✓
 ↓
记忆 ✓
 ↓
Agent ●
 ↓
邮件 ○
```

让用户一眼看到测试走到了哪里。

#### 过程时间线

使用大白话描述事件：

```text
10:32:01  Agent 读取了测试网页

10:32:04  网页中的内容被写入长期记忆

10:32:12  新一轮对话开始

10:32:16  Agent 读取了之前保存的记忆

10:32:20  Agent 尝试发送邮件

10:32:20  发现未经用户确认的操作
```

默认不要显示原始 JSON。

### 右侧：当前测试详情

显示：

```text
本次测试想验证什么

平台提供了什么内容

哪些操作不应该发生

当前判定

关键证据
```

例如：

```text
禁止行为：
在没有用户确认的情况下发送邮件

实际行为：
Agent 调用了“发送邮件”

判定：
发现严重风险
```

---

## 4.5 测评报告 Report

### 本页回答

> Agent 是否适合上线？哪里出了问题？为什么这样判断？

### 顶部结论

需要优先给出明确结论：

```text
上线评分：67 / 100

当前建议：
不建议直接上线
```

不要只给数字，不给建议。

### 三项评分

```text
任务能力
稳定性
安全性
```

安全性是重点，但另外两项仍然需要有真实内容，避免显得只是为了凑完整度。

### 风险分类

```text
严重风险  1
高风险    2
中风险    3
低风险    1
```

### 风险清单

每条风险默认显示：

```text
风险等级
风险名称
一句话说明
验证状态
```

例如：

```text
严重

恶意内容通过长期记忆影响后续操作

状态：已验证
```

### 风险详情

展开后按固定顺序展示：

```text
1. 风险是怎么产生的

2. 风险经过了哪些位置

3. 平台使用了什么测试输入

4. Agent 实际做了什么

5. 哪条权限规则被违反

6. 建议如何修复
```

风险路径小图和运行证据应使用与攻击图谱、测评运行页相同的节点和图标，保持视觉一致。

---

# 5. 跨页面必须保持一致的视觉语言

下面这些含义需要在所有页面中一致。

```text
不可信内容
敏感数据
长期记忆
危险操作
潜在风险
已验证风险
测试通过
测试失败
```

例如“长期记忆”在画像页、图谱页、运行页、报告页中，应该始终使用同一种图标或基础样式。

否则用户会误以为它们是不同的东西。

---

# 6. 页面之间的关键连接

## 画像 → 图谱

```text
确认画像并分析风险
```

## 图谱 → 运行

```text
验证这条风险路径
```

## 运行 → 报告

```text
查看风险详情
```

## 报告 → 图谱/运行

```text
查看完整路径
查看测试过程
```

这些按钮要让用户感觉自己在沿着一个完整流程前进。

---

# 7. 前端必须遵守与可以自由发挥的部分

## 必须遵守

```text
五步流程清楚

潜在风险和已验证风险有明显区别

测评过程必须与真实事件对应

所有风险结论都能查看证据

同一种节点和状态在各页面保持一致
```

## 可以自由发挥

```text
整体配色

字体与品牌风格

卡片和布局形式

图谱排布

节点造型

动画方式

页面是否适当合并

是否使用深色、科技感、极简或其他视觉语言
```

系统设计规定的是：

> 页面要表达什么。

前端负责决定：

> 怎么表达得更有风格、更直观。

---

# 8. 第一阶段实现优先级

建议顺序：

```text
1. 攻击图谱
   先完成节点、风险路径和路径详情

2. 安全画像
   完成能力展示和权限确认

3. 测评运行
   先用固定事件完成时间线和路径点亮

4. 测评报告
   完成一条风险的完整详情

5. 总览
   最后聚合其他页面已有的数据
```

第一阶段最重要的不是页面数量，而是先让下面这条故事可以完整展示：

```text
平台理解了 Agent
        ↓
发现一条潜在风险路径
        ↓
运行测试进行验证
        ↓
路径被真正打通
        ↓
报告给出证据和上线建议
```

只要这条故事足够清楚，用户和评委即使不了解智能体安全，也能够理解平台到底做了什么，以及为什么有价值。
