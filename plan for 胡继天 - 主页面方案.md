# 主页面方案(胡继天专用)

> 本文回答一个问题:**确认接入之后,主页面到底长什么样。**
> 依据:design V0.2 §25–27、`plan for 胡继天.md`、stage1_goal.md。
> 不需要懂智能体安全,全文用"体检"类比,照着做即可。

---

# 0. 先说结论:你的 agent 方案为什么显得复杂

你的 agent 把两类东西混在了一起:

```text
A. 给用户/评委看的产品页面     → 主页面要的
B. 给工程师调试用的内部视图   → 不该出现在主导航
```

9 个导航项里,有 4 个属于 B 类(安全资产库、契约状态、运行时间线、报告证据)。
砍掉/合并后,主导航只剩 **5 项**,而且这 5 项是一条线,不是一堆平级页面。

---

# 1. 一句话理解主页面

把平台想象成一家**智能体体检中心**。你已经做完了"挂号登记"(登录 + 参数页),
主页面就是接下来的一次完整体检:

```text
挂号(已做)
  ↓
① 总览        —— 我的体检进行到哪一步了?
  ↓
② 核对信息    —— 平台理解的"我的 Agent"对不对?(安全画像)
  ↓
③ 拍片子      —— 结构上哪里有毛病?(攻击图谱)
  ↓
④ 做检查      —— 医生正在怎么检查它?(测评运行)
  ↓
⑤ 拿报告      —— 结论是什么,哪里有问题?(测评报告)
```

评委(用户)在主页面只关心三个问题(来自你的 plan 文件 §2):

```text
① 我的 Agent 是什么结构?     → 攻击图谱 回答
② 平台正在怎么测它?          → 测评运行 回答
③ 到底哪里有问题?            → 测评报告 回答
```

任何页面区块,如果三个问题一个都回答不了,就是多余的,删掉。

---

# 2. 整体布局:顶栏 + 左侧导航 + 内容区

## 2.1 顶栏(从左到右)

```text
[Logo/产品名]  [当前 Agent 胶囊]        [步骤指示]        [健康点] [Mock开关] [导出报告] [头像]
```

| 元素          | 说明                      | 备注                      |
| ----------- | ----------------------- | ----------------------- |
| 当前 Agent 胶囊 | 名字 + 小状态点(已接入/分析中/测评中)  | 点击可切换 Agent 或重新接入       |
| 步骤指示        | 画像 → 图谱 → 测评 → 报告,当前步高亮 | 可选做;做了能很强化"体检流程"感       |
| 健康点         | 绿/灰小圆点,表示后端是否在线         | 悬停显示"后端正常/未连接"即可,不要做成页面 |
| Mock 开关     | Mock 数据 / 真实 API 切换     | **仅开发期可见,演示前隐藏**        |
| 导出报告        | 按钮                      | 只在报告页可用,其余页面灰掉          |
| 头像          | 登录已有                    | 不做个人中心                  |

## 2.2 左侧导航(5 项,顺序 = 体检流程)

```text
① 总览       Dashboard
② 安全画像    Profile
③ 攻击图谱    Anatomy
④ 测评运行    Run
⑤ 测评报告    Report
```

导航做成"步骤条"样式:完成一步点亮一步,可以回退查看。
这样评委一眼看懂:这不是后台管理系统,是一条测评流水线。

---

# 3. 五个页面逐个详述

每页按同一格式写:**回答什么 / 页面里有什么 / 用户能点什么 / 数据从哪来 / Stage 1 做到哪**。

---

## 3.1 总览 Dashboard

**回答:** 我现在进行到哪一步?下一步该干嘛?

**页面区块:**

```text
[当前 Agent 卡片]     名称、一句话描述、来源(本平台的 CorpMate / 外部 API)、接入时间
[体检进度]           画像 ✓ → 图谱 ✓ → 测评 ●运行中 → 报告 ○未生成
[四个关键数字]        总体分数 | 发现风险数(Critical红/High橙/Medium黄/Low灰 四色小方块)
                     | 已运行测试数 | 最高风险名称
[下一步大按钮]        引导到第一个未完成的步骤,例如「开始解剖」「启动测评」
```

**用户操作:** 点"下一步"跳转对应步骤;点风险数字跳到报告页。

**数据从哪来:** 前期全部用 fixture(`evaluation_report.json`);它是聚合页,等别的页都有数据了它自然就有。

**Stage 1 做到哪:** 静态 Mock 即可。**最后做**——它依赖其他所有页面,先做这个等于先装修客厅再盖房。

---

## 3.2 安全画像 Profile

**回答:** 平台理解的"我的 Agent"对不对?权限边界要不要调?

> 你的参数页是"**用户填**",本页是"**平台整理出来 + 用户确认**"。两步合起来就是 design 里的
> 「AI 提议,用户确认」。

**页面区块:**

```text
[能力清单]      它会做什么:读邮件 / 发邮件 / 浏览网页……用标签 chips 展示
[数据源]        它能看到什么外部内容:网页、邮件。
               ★ 用醒目样式标出「不可信来源」——这是后面所有攻击的入口
[记忆卡片]      有没有记忆?是否长期保存?
               ★ 用一张单独的醒目卡片——它是旗舰攻击的关键一环
[工具权限表]    每个工具一行:工具名 | ALLOW / CONFIRM / DENY 三档选择
               ★ 本页核心交互
[底部主按钮]    「确认画像并开始分析」→ 跳到攻击图谱页
```

**用户操作:** 改权限档位;确认画像。

**数据从哪来:** `POST /agents`(提交参数页的草稿)→ `GET /agents/{id}` 拿画像。
Stage 1 期间用 fixture(`agent_manifest.json`)先顶着。

**Stage 1 做到哪:** 这就是 F1-2,**本周第二个做**,权限三档编辑必须可用。

---

## 3.3 攻击图谱 Anatomy(核心页 1/3,Stage 1 的主角)

**回答:** 我的 Agent 结构上,哪里有一条可被攻击的路?

**页面区块:**

```text
[中央:图]
  五类节点:来源 / 智能体 / 工具 / 记忆 / 数据——用不同形状或颜色区分(视觉你定)
  节点上的安全标签(不可信/敏感/危险/持久)用角标或小色点表达

[风险路径]
  命中的风险路径整条高亮(加粗/亮色);
  侧边有路径列表,多条路径可切换查看;
  每条路径配一张卡片:风险名 + 等级徽章(CRITICAL 红……)+ 一句大白话解释,例如
  「恶意网页的内容可以被存进记忆,之后的对话里诱导 Agent 发出你没要求的邮件」

[节点详情]
  点击节点/边 → 右侧抽屉:类型、标签、一句话说明

[顶部小按钮]
  「开始分析」(从画像页确认后跳转过来时自动触发也行)
```

**用户操作:** 看图、点节点、切换路径。

**数据从哪来:** `GET /agents/{id}/graph`(AttackGraph + risk_path_ids)。
这是 SR2 唯一要切到真实 API 的端点。

**Stage 1 做到哪:** F1-3,**本周第一个做**。
M1 验收(S7 串跑 SR3)时,评委就是在这页看到
`Web → Agent → Memory → Agent → Email` 这条红色路径——**这页是全平台的门面,值得投入最多功夫。**

---

## 3.4 测评运行 Run(核心页 2/3,Stage 1 搭壳,Stage 2/3 才是主场)

**回答:** 平台现在正在怎么测/攻击我的 Agent?

**布局:左中右三栏**

```text
[左栏:测试清单]
  每条测试一行:测试名 | 类别 | 状态(排队 ○ / 运行中 ● / 通过 ✓ / 失败 ✗)

[中栏:过程区,两种看法可切换]
  a. 时间线:事件流——「调用了什么工具」「写入了什么记忆」「判定:失败」
     ★ 你 agent 方案里的「运行时间线」放这里,它是一个组件,不是一个页面
  b. 图谱联动:图谱缩略图,攻击走到哪个节点就亮哪个
     (Stage 3 旗舰动画放这;Stage 1 不用做)

[右栏:当前测试详情]
  测试输入 | 禁止动作 | 判定结果 | 证据(哪一步违反了规则)

[顶部]
  进度(3/12)+「启动评估」按钮
```

**用户操作:** 选测试、启动、看过程、点失败项看证据。

**数据从哪来:** `POST /evaluations` 创建测评;`GET /evaluations/{id}/events` 拿过程事件;
`GET /evaluations/{id}/findings` 拿结果。

**Stage 1 做到哪:** **只搭静态壳**:左栏放几条假测试,中栏时间线展示一条已跑完的测试记录
(M1 串跑要演示"一条测试真实跑出 FAIL 且带证据"),图谱联动不做。实时事件流是 Stage 2 的事。

---

## 3.5 测评报告 Report(核心页 3/3)

**回答:** 结论是什么?哪里有问题?凭什么这么说?

**页面区块:**

```text
[顶部结论卡]      上线分数 + 上线建议(如「不建议上线」红色横幅)
                + 四类风险计数(Critical/High/Medium/Low)
[三项分]         能力 / 可靠性 / 安全性——安全性视觉最重,另外两个不能像占位符
[风险清单]        每条风险一行:等级徽章 | 风险名 | 一句话说明
[风险详情]        点击某条展开:
                · 风险路径小图(哪几个节点连成的线)
                · 攻击输入(攻击者给它看了什么)
                · 关键证据(第几步、调用了哪个不该调用的工具)
                · 违反了哪条规则
                · 修复建议(先用一句大白话,后端后续会给正式文案)
```

**用户操作:** 看结论、逐条展开风险、导出报告。

**数据从哪来:** `GET /evaluations/{id}/report` + `/findings`;前期用 fixture(`evaluation_report.json`)。

**Stage 1 做到哪:** fixture 静态展示,能展开一条风险详情即可。

---

# 4. 对你 agent 方案的裁剪对照

## 4.1 导航:9 项 → 5 项

| 原导航项     | 去向          | 原因                                                         |
| -------- | ----------- | ---------------------------------------------------------- |
| 总览       | ✅ 保留        | 第①步                                                        |
| Agent 接入 | ↩️ 移出主导航    | 接入是进主页面**之前**的事(你已做);主页面只留顶栏"切换/重新接入"入口                    |
| 安全画像     | ✅ 保留        | 第②步                                                        |
| 攻击图谱     | ✅ 保留        | 第③步,门面                                                     |
| 安全资产库    | ❌ 砍掉        | 这是陈书扬的"弹药库"(风险模式/攻击种子),工程资产不是用户页面;评委不问就不展示。后期真想要,放二级"实验室"页 |
| 测试工作台    | 🔀 并入「测评运行」 | 选测试就是运行页左栏,不需要独立页                                          |
| 运行时间线    | 🔀 并入「测评运行」 | 它是中栏的一个**组件**,不是页面                                         |
| 报告证据     | 🔀 并入「测评报告」 | 证据是风险详情的下半部分                                               |
| 契约状态     | ❌ 砍掉        | 这是给我们自己看的工程守则(CODING_AGENT_RULE),对评委无意义;后端健康浓缩成顶栏一个圆点      |

## 4.2 功能栏:11 项 → 4 个全局 + 其余各归各页

| 原功能项           | 去向                             |
| -------------- | ------------------------------ |
| 当前 Agent 选择    | ✅ 全局,顶栏胶囊                      |
| 健康状态           | ✅ 全局,顶栏圆点                      |
| 数据源模式(Mock/真实) | ✅ 全局,但仅开发期可见                   |
| 导出报告           | ✅ 全局,仅报告页可用                    |
| 开始分析           | ➡️ 页面级按钮,放画像页/图谱页              |
| 启动评估           | ➡️ 页面级按钮,放运行页                  |
| 重置沙箱           | ➡️ 运行页,Stage 2 再说,Stage 1 不做   |
| 风险筛选           | ➡️ 报告页内的筛选器                    |
| 节点标签筛选         | ➡️ 图谱页内的筛选器                    |
| 事件类型筛选         | ➡️ 时间线组件内的筛选器                  |
| 视图切换(JSON)     | ❌ 砍掉;最多留个 `< >` 小图标给开发调试,演示不出现 |

原则:**全局栏只放"任何时候都可能点"的;其余都是某个页面自己的事。**

---

# 5. 实施顺序(对齐 stage1_goal,D3–D7)

| 顺序  | 做什么                        | 对应任务       | 验收节点              |
| --- | -------------------------- | ---------- | ----------------- |
| 1   | 攻击图谱页(fixture 数据 + 风险路径高亮) | F1-3       | SR2:切真实 graph API |
| 2   | 安全画像页(权限三档编辑可用)            | F1-2       | SR3 走查            |
| 3   | 测评运行页静态壳 + 报告页 fixture 展示  | M1 演示需要    | SR3 走查            |
| 4   | 总览页(聚合 Mock)               | 锦上添花       | 有时间就做             |
| 全程  | 顶栏 + 左侧步骤导航骨架              | 先搭出来,各页往里填 | ——                |

提醒(来自 stage1_goal 坑 2):**全程 fixture 先行,不要等后端**;
SR2 联调只切 `GET /agents/{id}/graph` 一个端点,其他页面继续吃 fixture。

---

# 6. 三条最后提醒

```text
① 主页面讲的是一个线性故事:画像 → 图谱 → 运行 → 报告。
  评委 3 分钟要能走完。九个平级管理页讲不出故事,五个步骤页可以。

② 不知道某个区块该不该放时,问它回答三个问题里的哪一个
  (结构?/怎么测?/哪里有问题?)。三个都不回答 → 删。

③ 不懂术语没关系,页面文案先用大白话:
  不写「Persistent Indirect Prompt Injection」,
  先写「恶意网页可以通过记忆,在之后的对话里操纵 Agent 发邮件」,
  术语放小字或悬停提示里。评委先听懂,再觉得专业。
```
